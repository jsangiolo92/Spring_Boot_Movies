server:
  port: 4000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/movies
    username: demo
    password: postres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
